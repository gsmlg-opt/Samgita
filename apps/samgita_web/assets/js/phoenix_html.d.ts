// Type definitions for Phoenix HTML JavaScript
// Handles method=PUT/DELETE in forms and buttons

declare module "phoenix_html" {
  // phoenix_html doesn't export anything, it just adds event listeners
  // when imported to handle Rails-style HTTP method overrides
}
